{
  "name": "kulala-core-consumer-app-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run ../../packages/core/scripts/generate-vendored-curl.ts && bun build src/main.ts --compile --compile-autoload-package-json --define __KULALA_EMBED_CURL__=true --outfile dist/kulala-core-consumer-app",
    "run": "bun run build && ./dist/kulala-core-consumer-app"
  },
  "dependencies": {
    "@mistweaverco/kulala-core": "workspace:*"
  }
}
