{
  "name": "kulala-core-cli-example",
  "private": true,
  "type": "module",
  "dependencies": {
    "@mistweaverco/kulala-core": "workspace:*"
  },
  "scripts": {
    "build": "bun run ../../packages/core/scripts/generate-vendored-curl.ts && bun build src/main.ts --compile --define __KULALA_EMBED_CURL__=true --outfile dist/kulala-core-cli",
    "run": "bun run build && ./dist/kulala-core-cli"
  }
}
