{
  "name": "@kulala/core",
  "type": "module",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mistweaverco/kulala-core.git"
  },
  "homepage": "https://github.com/mistweaverco/kulala-core#readme",
  "bugs": {
    "url": "https://github.com/mistweaverco/kulala-core/issues"
  },
  "engines": {
    "bun": "1.3.14"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "scripts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --check . && eslint .",
    "test": "bun test",
    "build": "bun run build:js && bun run build:types",
    "build:js": "bun build src/index.ts --outdir dist --format esm --target bun --sourcemap --define __KULALA_EMBED_CURL__=false --define __KULALA_EMBED_JQ__=false",
    "build:types": "tsc -p tsconfig.build.json",
    "prepack": "cp ./../../README.md . && cp ./../../LICENSE . && bun run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "version": "0.0.0"
}
